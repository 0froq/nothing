---
title: 安装
description: 一条命令，把 void 加进任何项目。
---

## 环境要求

任何能「什么都不做」的运行时都可以：Node.js、Deno、Bun 和浏览器都符合。

## 安装

```bash
pnpm add void
```

## 验证

```ts
import { run } from 'void'

run() // undefined
```

如果调用返回 `undefined`，说明 void 安装正确；如果返回了别的东西，说明你装的是别的东西。
