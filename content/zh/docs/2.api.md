---
title: API
description: 完整的 API，包括所有选项。
---

## `run()`

运行 void。不接收参数，返回 `undefined`。

```ts
function run(): void
```

## 选项

| 选项 | 类型 | 默认值 |
| ---- | ---- | ------ |
| 无   | —    | —      |

## 错误

void 不会抛错。没有代码路径，也就没有失败路径。
